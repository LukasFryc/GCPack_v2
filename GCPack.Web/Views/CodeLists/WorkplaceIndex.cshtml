@model IEnumerable<GCPack.Model.WorkplaceModel>
@{
    ViewBag.Title = "CodeListsController";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Lokality</h2>
@Html.ActionLink("Přidat", "WorkplaceAdd", "CodeLists", new { @class = "btn btn-default btn-sm" })
<div class="row">
    <div class="col-md-6">
        <table class="table" style="margin-top:20px">
            <tr>
                <th>
                    Kód
                </th>
                <th>
                    Název
                </th>
                <th></th>
            </tr>

            @foreach (var m in @Model)
            {
                <tr>
                    <td>@m.Code</td>
                    <td>@m.Name (@m.ID)</td>
                    <td>
                        @Html.ActionLink("Upravit", "WorkplaceEdit", "CodeLists", new { ID = m.ID }, new { @class = "btn btn-default btn-sm" })
                        @Html.ActionLink("Smazat", "WorkplaceDelete", "CodeLists", new { ID = m.ID }, new { @class = "btn btn-default btn-sm" })
                    </td>
                </tr>
            }
        </table>
    </div>
</div>
