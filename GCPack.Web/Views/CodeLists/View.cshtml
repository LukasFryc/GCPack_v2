@model GCPack.Model.AppSystemModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@ViewBag.Title</h2>
@using (Html.BeginForm("SaveAppSystem", "CodeLists", FormMethod.Post))
{
    <div class="container">
        <input type="hidden" name="ID" value="@Model.ID" />
        <div class="row">
            <div class="col-md-2">Název</div>
            <div class="col-md-6">
                @Html.EditorFor(m => m.Name, new { htmlAttributes = new { @class = "form-control", @placeholder = "Hodnota položky system" } })
            </div>
        </div>

        <div class="row">
            <div class="col-md-2"><input type="submit" class="btn btn-success" /></div>
        </div>
    </div>
}
